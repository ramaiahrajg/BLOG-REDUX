const initialState = {
    posts : [
        {
            id: 'Test Prop'
        }
],
    ajaxCalls : {
        getAllPosts : {
            loading :false,
            hasError : false,
        }
    }
};

export default initialState;